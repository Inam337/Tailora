import { useEffect } from 'react';

/**
 * Placeholder for offline outbox sync when connectivity returns.
 */
export function useAutoSyncWhenOnline() {
  useEffect(() => {
    // Intentionally empty.
  }, []);
}
